@model IEnumerable<musicApp.Models.PlaylistSong>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.PlaylistId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SongId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.OrderPosition)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.PlaylistId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SongId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.OrderPosition)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PlaylistSongId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PlaylistSongId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PlaylistSongId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
