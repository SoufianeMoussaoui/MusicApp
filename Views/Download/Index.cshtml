@model IEnumerable<musicApp.Models.Download>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.SongId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DownloadedAt)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.SongId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DownloadedAt)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DownloadId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DownloadId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DownloadId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
